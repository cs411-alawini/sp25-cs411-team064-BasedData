<!DOCTYPE html>
<html>
<head>
  <title>Diagnosis</title>
</head>
<body>
  <h1>Diagnosis for <%= user.name %></h1>
  <p>You reported the following symptoms: <%= inputSymptoms.join(', ') %></p>
  <% if (diseases.length > 0) { %>
    <h2>Possible Diseases:</h2>
    <ul>
      <% diseases.forEach(disease => { %>
        <li>
          <strong><%= disease.Disease_Name %></strong> - Matches: <%= disease.match_count %>
        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No matching diseases found. Please consult a healthcare professional.</p>
  <% } %>
  <a href="/delete/<%= user.userId %>" onclick="return confirm('Are you sure you want to delete your data?');">Delete My Data</a>
</body>
</html>
